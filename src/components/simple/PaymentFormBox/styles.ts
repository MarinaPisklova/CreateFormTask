import styled from "styled-components";
import { Title } from "../../../styles";
import SecureMessage from "../SecureMessage/SecureMessage";

export const StyledSecureMessage = styled(SecureMessage)`
  margin-bottom: 15px;
`

export const StyledTitle = styled(Title)`
  margin-bottom: 15px;
`
