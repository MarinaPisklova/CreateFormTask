import styled from "styled-components"
import { Flex } from "../../../styles"

export const SecureMessageText = styled.p`
  font-size: 14px;
  line-height: 18px;
  color: ${props => props.theme.colors.darkGray};
`

export const StyledFlex = styled(Flex)`
  margin-bottom: 19px;
`
